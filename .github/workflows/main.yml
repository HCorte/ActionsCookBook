#a first workflow to test things
name: My First Workflow
on: 
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  first_job:
    name: My first job
    runs-on: ubuntu-latest
    steps:
      - name: Greet the user
        run: |
          echo "Hello world from ${{ github.actor }}."
          ech0 "Current branch is '${{ github.ref }}'."
        shell: bash
    
  
  
